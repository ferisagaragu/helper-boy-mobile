import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.pechblenda.helperboymobile',
  appName: 'Pequeño ayudador',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
